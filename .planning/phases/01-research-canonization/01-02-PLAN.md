---
phase: 01-research-canonization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["documents/canonical_naming_rules.md"]
autonomous: true
gap_closure: true
requirements: ["DOC-01", "DOC-02"]

must_haves:
  truths:
    - "Developers can derive one canonical full name for every supported exponent band from 1e0 through 1e2999999."
    - "Developers can derive one canonical abbreviation for every supported exponent band from 1e0 through 1e2999999."
    - "Boundary cases such as 1e3003 and 1e6003 have a single documented output with no alternatives."
  artifacts:
    - path: "documents/canonical_naming_rules.md"
      provides: "Deterministic naming and abbreviation specification for the supported exponent range."
  key_links:
    - from: "documents/canonical_naming_rules.md"
      to: "templates/js/scripts.js"
      via: "documented formulas and worked examples copied from the current formatter baseline"
      pattern: "suffixIndex = floor\\(exponent / 3\\) - 1|10\\^6003 -> Dumillillion|10\\^6003 -> DMi"
---

<objective>
Close the documentation gaps found in Phase 1 verification by rewriting the canonical naming rules into a deterministic specification that exactly covers the supported exponent range and the current formatter baseline.

Purpose: Phase 2 cannot build authoritative tests until the naming and abbreviation rules are mathematically bounded and unambiguous.
Output: Updated canonical naming document with exact formulas, explicit in-range limits, and single-output worked examples.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-canonization/01-CONTEXT.md
@.planning/phases/01-research-canonization/01-RESEARCH.md
@.planning/phases/01-research-canonization/01-VERIFICATION.md
@templates/js/scripts.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite canonical full-name coverage and range limits</name>
  <files>documents/canonical_naming_rules.md</files>
  <read_first>documents/canonical_naming_rules.md, templates/js/scripts.js, .planning/phases/01-research-canonization/01-VERIFICATION.md, .planning/phases/01-research-canonization/01-CONTEXT.md, .planning/phases/01-research-canonization/01-RESEARCH.md</read_first>
  <action>Rewrite documents/canonical_naming_rules.md so it becomes a deterministic specification for the current formatter instead of a high-level note. Add exact sections named `## Scope and Supported Ceiling`, `## Canonical Full-Name Algorithm`, and `## Worked Full-Name Examples`. In `## Scope and Supported Ceiling`, derive the supported limit with the exact formula `suffixIndex = floor(exponent / 3) - 1`, show that the maximum supported exponent `1e2999999` yields `suffixIndex = 999998`, `t2Index = 999`, and `t1Index = 998`, and state explicitly that no Tier 3 naming is required inside project scope because Tier 3 would start at `1e3000003`, which is out of range. In `## Canonical Full-Name Algorithm`, document the exact current runtime behavior from templates/js/scripts.js: exponents below 3 map to `Under a Thousand`; suffix indices below 1000 use the Tier 1 arrays `basics`, `units`, `tens`, and `hundreds`; suffix indices from 1000 through 999998 split into `t2Index = floor(suffixIndex / 1000)` and `t1Index = suffixIndex % 1000`, use `milliOnes` plus lowercase `tens` and `hundreds` plus `milli` to form the prefix, and append `llion` when `t1Index === 0` or the lowercase Tier 1 name otherwise. Remove the unsupported `1e3000003` example and any claims that Tier 3 or higher names are already specified. In `## Worked Full-Name Examples`, include the exact canonical outputs `10^3 -> Thousand`, `10^6 -> Million`, `10^33 -> Decillion`, `10^3003 -> Millillion`, and `10^6003 -> Dumillillion`.</action>
  <acceptance_criteria>`documents/canonical_naming_rules.md` contains `## Scope and Supported Ceiling`; it contains the exact text `suffixIndex = floor(exponent / 3) - 1`; it states `1e3000003` is out of scope for Phase 1; it contains `10^3003 -> Millillion`; it contains `10^6003 -> Dumillillion`; it no longer contains `Micrillion (1e3000003)`.</acceptance_criteria>
  <verify>
    <automated>grep -q "## Scope and Supported Ceiling" documents/canonical_naming_rules.md && grep -q "suffixIndex = floor(exponent / 3) - 1" documents/canonical_naming_rules.md && grep -q "10\^6003 -> Dumillillion" documents/canonical_naming_rules.md && ! grep -q "Micrillion (1e3000003)" documents/canonical_naming_rules.md</automated>
  </verify>
  <done>The canonical document fully defines the supported full-name algorithm and mathematically proves why the project scope stops before any Tier 3 naming is needed.</done>
</task>

<task type="auto">
  <name>Task 2: Define a single abbreviation algorithm and parity table</name>
  <files>documents/canonical_naming_rules.md</files>
  <read_first>documents/canonical_naming_rules.md, templates/js/scripts.js, .planning/phases/01-research-canonization/01-VERIFICATION.md</read_first>
  <action>Add exact sections named `## Canonical Abbreviation Algorithm`, `## Name-to-Abbreviation Parity Rules`, and `## Worked Abbreviation Examples`. Document the runtime abbreviation rules exactly as implemented in templates/js/scripts.js: suffix indices `0`, `1`, and `2` use `K`, `M`, and `B`; suffix indices `3` through `999` use `getSmallSuffixChunk(index)` built from `firstOnes`, `secondOnes`, and `thirdOnes`; suffix indices `1000` through `999998` use `getLargeSuffixChunk(floor(suffixIndex / 1000) - 1) + "Mi" + getSmallSuffixChunk(suffixIndex % 1000)`, and state explicitly that only the `Mi` tier token appears inside the supported range because `maxGroup` never exceeds `1` below `1e3000000`. Add a parity table that pairs each worked full-name example with one abbreviation only. Include the exact outputs `10^3 -> K`, `10^6 -> M`, `10^33 -> De`, `10^3003 -> Mi`, and `10^6003 -> DMi`. Remove any wording that allows multiple abbreviations for the same exponent.</action>
  <acceptance_criteria>`documents/canonical_naming_rules.md` contains `## Canonical Abbreviation Algorithm`; it contains the exact text `getLargeSuffixChunk(floor(suffixIndex / 1000) - 1) + "Mi" + getSmallSuffixChunk(suffixIndex % 1000)`; it contains `10^3003 -> Mi`; it contains `10^6003 -> DMi`; it does not contain `DiMi` or `BiMi` as allowed alternatives.</acceptance_criteria>
  <verify>
    <automated>grep -q "## Canonical Abbreviation Algorithm" documents/canonical_naming_rules.md && grep -q 'getLargeSuffixChunk(floor(suffixIndex / 1000) - 1) + "Mi" + getSmallSuffixChunk(suffixIndex % 1000)' documents/canonical_naming_rules.md && grep -q "10\^6003 -> DMi" documents/canonical_naming_rules.md && ! grep -q "DiMi" documents/canonical_naming_rules.md && ! grep -q "BiMi" documents/canonical_naming_rules.md</automated>
  </verify>
  <done>The canonical document defines one abbreviation algorithm for the entire supported range and one canonical abbreviation for each documented edge case.</done>
</task>

</tasks>

<verification>
The updated document must satisfy DOC-01 and DOC-02 without relying on unstated Tier 3 assumptions, and it must provide deterministic outputs for the edge examples called out by verification.</verification>

<success_criteria>
Phase 1 has a gap-closure plan that updates the canonical naming document into a mathematically bounded, unambiguous test oracle for full names and abbreviations through 1e2999999.</success_criteria>

<output>
After completion, create `.planning/phases/01-research-canonization/01-02-SUMMARY.md`
</output>