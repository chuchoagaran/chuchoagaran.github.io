---
phase: 02-test-harness-construction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["scripts/tests/formatter_vectors.json", "scripts/references/test_formatter.js"]
autonomous: true
requirements: ["TEST-01", "TEST-02"]

must_haves:
  truths:
    - "Developers can run a Node.js harness from repo root and verify canonical readable-name snapshots."
    - "Developers can run the same harness and verify canonical abbreviation snapshots."
    - "Boundary transitions around Tier 2 exponents are checked automatically instead of by manual inspection."
  artifacts:
    - path: "scripts/tests/formatter_vectors.json"
      provides: "Shared canonical test vectors for readable names, abbreviations, and normalization cases."
    - path: "scripts/references/test_formatter.js"
      provides: "Executable Node harness against the real browser formatter source."
  key_links:
    - from: "scripts/references/test_formatter.js"
      to: "templates/js/scripts.js"
      via: "vm-based source loading with the corrected ../../templates/js/scripts.js path"
      pattern: "path\.join\(__dirname, '..', '..', 'templates', 'js', 'scripts\.js'\)"
    - from: "scripts/references/test_formatter.js"
      to: "scripts/tests/formatter_vectors.json"
      via: "fixture-driven assertions and boundary windows"
      pattern: "formatter_vectors\.json|boundaryWindows"
---

<objective>
Create the authoritative JS-side test harness for Phase 2 by defining shared canonical fixtures and making the Node verification script execute the production formatter deterministically.

Purpose: Phase 3 cannot trust test failures until Phase 2 has one fixture source and one working JS harness that exercises the real runtime behavior.
Output: Shared formatter fixture file and a corrected Node harness that asserts names, abbreviations, normalization, and boundary windows.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-canonization/01-02-SUMMARY.md
@.planning/phases/02-test-harness-construction/02-RESEARCH.md
@.planning/phases/02-test-harness-construction/02-VALIDATION.md
@documents/canonical_naming_rules.md
@templates/js/scripts.js
@scripts/references/test_formatter.js
@scripts/tests/test_formatter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the shared canonical formatter vector contract</name>
  <files>scripts/tests/formatter_vectors.json</files>
  <read_first>documents/canonical_naming_rules.md, templates/js/scripts.js, scripts/references/test_formatter.js, scripts/tests/test_formatter.py</read_first>
  <action>Create `scripts/tests/formatter_vectors.json` as the shared contract for every Phase 2 check. Use a top-level object with exact keys `schemaVersion`, `cases`, `boundaryWindows`, and `sampleRanges`. Each item in `cases` must contain `id`, `mantissa`, `exponent`, `expectedFullName`, `expectedAbbreviation`, `expectedReadableText`, and `expectedAbbreviationText`. Include at least these exact canonical cases from the current documentation and runtime behavior: `e3` -> `Thousand` / `K`, `e6` -> `Million` / `M`, `e33` -> `Decillion` / `De`, `e3003` -> `Millillion` / `Mi`, `e3006` -> `Millimillion` / `MiU`, `e6003` -> `Dumillillion` / `DMi`, and the normalization case `mantissa: 1, exponent: 3005` with `expectedReadableText: "100 Millillion"` and `expectedAbbreviationText: "100 Mi"`. In `boundaryWindows`, add exact windows for `2994..3015`, `5994..6015`, `8994..9015`, and `29994..30015`. In `sampleRanges`, define sparse parity ranges that stay within the supported ceiling, including one range under 1000, one around the first Tier 2 boundary, and one in the 100000..1000000 band.</action>
  <acceptance_criteria>`scripts/tests/formatter_vectors.json` exists; it contains `"schemaVersion": 1`; it contains `"id": "e3003"`; it contains `"expectedFullName": "Millillion"`; it contains `"expectedAbbreviation": "DMi"`; it contains `"expectedReadableText": "100 Millillion"`; it contains `"startExponent": 2994` and `"endExponent": 3015`.</acceptance_criteria>
  <verify>
    <automated>grep -q '"schemaVersion": 1' scripts/tests/formatter_vectors.json && grep -q '"id": "e3003"' scripts/tests/formatter_vectors.json && grep -q '"expectedReadableText": "100 Millillion"' scripts/tests/formatter_vectors.json && grep -q '"startExponent": 2994' scripts/tests/formatter_vectors.json</automated>
  </verify>
  <done>The repository has one shared fixture source that encodes the canonical formatter outputs and boundary windows for Phase 2.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the Node harness around production logic and shared fixtures</name>
  <files>scripts/references/test_formatter.js</files>
  <read_first>scripts/references/test_formatter.js, scripts/tests/formatter_vectors.json, templates/js/scripts.js, documents/canonical_naming_rules.md</read_first>
  <action>Update `scripts/references/test_formatter.js` so it is the authoritative JS harness for the production formatter. Change the runtime source path to `path.join(__dirname, '..', '..', 'templates', 'js', 'scripts.js')` so it resolves from `scripts/references/` to the repo root correctly. Add fixture loading from `path.join(__dirname, '..', 'tests', 'formatter_vectors.json')`. Replace the hard-coded known-case arrays with fixture-driven assertions for `getFullNameFromExponent`, `getAbbreviationFromExponent`, `buildReadableText`, and `buildAbbreviationText`. Keep boundary uniqueness checks, but drive them from the fixture `boundaryWindows` array instead of manual literals. Add a `--json` mode using `process.argv.includes('--json')` that prints one JSON object containing `passed`, `failed`, `caseResults`, and `windowResults`, and exits with status `1` on any mismatch. When `--json` is absent, keep the human-readable console summary and side-by-side dumps.</action>
  <acceptance_criteria>`scripts/references/test_formatter.js` contains `path.join(__dirname, '..', '..', 'templates', 'js', 'scripts.js')`; it contains `formatter_vectors.json`; it contains `process.argv.includes('--json')`; it asserts `buildReadableText` and `buildAbbreviationText`; it references `boundaryWindows` from the fixture file.</acceptance_criteria>
  <verify>
    <automated>node scripts/references/test_formatter.js && node scripts/references/test_formatter.js --json | grep -q '"failed":0'</automated>
  </verify>
  <done>The Node harness executes production formatter logic from repo root, consumes the shared fixture contract, and returns machine-readable pass/fail results.</done>
</task>

</tasks>

<verification>
The Phase 2 JS harness is complete when repo-root Node commands prove the canonical name and abbreviation outputs, including Tier 2 edge windows, without broken path assumptions.</verification>

<success_criteria>
Phase 2 has a working JS-side snapshot harness that encodes the canonical formatter behavior for both names and abbreviations and can be consumed by later parity tooling.</success_criteria>

<output>
After completion, create `.planning/phases/02-test-harness-construction/02-01-SUMMARY.md`
</output>
