---
phase: 02-test-harness-construction
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["scripts/tests/test_formatter.py", "scripts/tests/test_formatter_parity.py"]
autonomous: true
requirements: ["TEST-03"]

must_haves:
  truths:
    - "Developers can run the Python helper and get the same canonical outputs as the JS formatter for shared cases."
    - "A cross-language parity command fails automatically when Python and JS drift apart."
    - "Phase 3 can treat JS-versus-Python mismatches as actionable regressions instead of ad-hoc diagnostics."
  artifacts:
    - path: "scripts/tests/test_formatter.py"
      provides: "Python-side formatter verification aligned with current JS behavior."
    - path: "scripts/tests/test_formatter_parity.py"
      provides: "Automated JS-versus-Python parity runner."
  key_links:
    - from: "scripts/tests/test_formatter.py"
      to: "scripts/tests/formatter_vectors.json"
      via: "shared canonical fixture loading"
      pattern: "formatter_vectors\.json"
    - from: "scripts/tests/test_formatter_parity.py"
      to: "scripts/references/test_formatter.js"
      via: "subprocess parity command using --json mode"
      pattern: "subprocess.run\(\[\"node\", \"scripts/references/test_formatter.js\", \"--json\"\]"
---

<objective>
Add the Python-side parity layer for Phase 2 so canonical fixtures are verified in both languages and JS/Python drift is detected automatically.

Purpose: The existing Python helper is a divergent port. Phase 2 must turn it into a reliable parity check against the real JS formatter before Phase 3 starts refactoring logic.
Output: Updated Python formatter helper plus an automated JS-versus-Python parity runner.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-canonization/01-02-SUMMARY.md
@.planning/phases/02-test-harness-construction/02-RESEARCH.md
@.planning/phases/02-test-harness-construction/02-VALIDATION.md
@documents/canonical_naming_rules.md
@templates/js/scripts.js
@scripts/tests/formatter_vectors.json
@scripts/references/test_formatter.js
@scripts/tests/test_formatter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the Python helper to match the current JS formatter exactly</name>
  <files>scripts/tests/test_formatter.py</files>
  <read_first>scripts/tests/test_formatter.py, scripts/tests/formatter_vectors.json, templates/js/scripts.js, documents/canonical_naming_rules.md</read_first>
  <action>Replace the divergent Python port logic in `scripts/tests/test_formatter.py` so it mirrors the current JS formatter exactly. Use `suffix_index = exponent // 3 - 1`, not `index = exponent // 3`. Add a `get_milli_prefix` helper that mirrors the JS `milliOnes` + lowercase `tens` + lowercase `hundreds` construction. Remove the old hyphenated `Tier2-prefix + suffix` behavior and the old `TIER2`-driven readable-name fallback. Update readable-name generation so `t1Index == 0` appends `llion` and other Tier 2 cases append `get_tier1_name(t1Index + 1).lower()`. Update abbreviation generation so it mirrors `getLuaStyleAbbreviationSuffix`, including the `i == 0` special case and the exact `get_large_suffix_chunk` increment logic. Add fixture loading from `scripts/tests/formatter_vectors.json` and assert every `expectedFullName`, `expectedAbbreviation`, `expectedReadableText`, and `expectedAbbreviationText` in the file. Keep the script executable from repo root with `python scripts/tests/test_formatter.py` and make it exit non-zero on any mismatch.</action>
  <acceptance_criteria>`scripts/tests/test_formatter.py` contains `suffix_index = exponent // 3 - 1`; it contains `def get_milli_prefix(`; it contains `formatter_vectors.json`; it no longer contains `return f"{t2_prefix}-{t1_suffix}"`; it exits 0 when run from repo root after the fixture-driven checks pass.</acceptance_criteria>
  <verify>
    <automated>python scripts/tests/test_formatter.py</automated>
  </verify>
  <done>The Python helper computes the same canonical outputs as the current JS formatter for the shared fixture cases and normalization examples.</done>
</task>

<task type="auto">
  <name>Task 2: Add an automated JS-versus-Python parity runner</name>
  <files>scripts/tests/test_formatter_parity.py</files>
  <read_first>scripts/tests/test_formatter.py, scripts/references/test_formatter.js, scripts/tests/formatter_vectors.json</read_first>
  <action>Create `scripts/tests/test_formatter_parity.py` as the repo-root cross-language parity command for `TEST-03`. The script must load `scripts/tests/formatter_vectors.json`, run `node scripts/references/test_formatter.js --json` through `subprocess.run([...], capture_output=True, text=True)`, parse the JSON results, compute the same outputs through the Python functions in `scripts/tests/test_formatter.py`, and compare JS versus Python values for every fixture case. Also iterate the `sampleRanges` entries from the fixture file and compare raw `get_full_name_from_exponent` and abbreviation outputs for those sampled exponents. Print clear mismatch lines that include the exponent, JS value, and Python value. Print `PARITY OK` and exit `0` when all comparisons match; exit non-zero otherwise.</action>
  <acceptance_criteria>`scripts/tests/test_formatter_parity.py` exists; it contains `subprocess.run(["node", "scripts/references/test_formatter.js", "--json"]`; it contains `PARITY OK`; it references `sampleRanges`; it exits 0 when JS and Python stay aligned.</acceptance_criteria>
  <verify>
    <automated>python scripts/tests/test_formatter_parity.py</automated>
  </verify>
  <done>The repository has one automated cross-language parity command that fails immediately when the Python helper and JS formatter drift apart.</done>
</task>

</tasks>

<verification>
The Phase 2 parity layer is complete when the Python helper matches the shared canonical fixture set and a single parity command compares Python outputs to the JS harness automatically.</verification>

<success_criteria>
Phase 2 has an automated cross-language parity workflow that protects the formatter from silent JS/Python drift before any Phase 3 refactor work begins.</success_criteria>

<output>
After completion, create `.planning/phases/02-test-harness-construction/02-02-SUMMARY.md`
</output>
