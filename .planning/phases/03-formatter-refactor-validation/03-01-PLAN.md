---
phase: 03-formatter-refactor-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["templates/js/scripts.js"]
autonomous: true
requirements: ["FMT-01", "FMT-02"]

must_haves:
  truths:
    - "Users get the same canonical full names for every documented and sampled exponent after the refactor."
    - "Users get the same canonical abbreviations for those same exponents after the refactor."
    - "Phase 2 JS and Python verification commands still pass after the formatter internals are reorganized."
  artifacts:
    - path: "templates/js/scripts.js"
      provides: "Refactored formatter core with shared exponent-to-group helpers for names and abbreviations."
  key_links:
    - from: "templates/js/scripts.js"
      to: "scripts/references/test_formatter.js"
      via: "getFullNameFromExponent, getAbbreviationFromExponent, buildReadableText, and buildAbbreviationText"
      pattern: "getFullNameFromExponent|getAbbreviationFromExponent|buildReadableText|buildAbbreviationText"
    - from: "templates/js/scripts.js"
      to: "scripts/tests/test_formatter_parity.py"
      via: "shared canonical outputs verified through the JS harness"
      pattern: "PARITY OK"
---

<objective>
Refactor the formatter core so canonical naming and abbreviation generation come from one internally consistent helper pipeline while preserving the existing public browser API.

Purpose: Phase 2 proved the current behavior; Phase 3 now needs to reduce drift risk in `templates/js/scripts.js` without changing the documented outputs.
Output: A cleaner formatter implementation that still passes every canonical snapshot and parity check.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-canonization/01-02-SUMMARY.md
@.planning/phases/02-test-harness-construction/02-01-SUMMARY.md
@.planning/phases/02-test-harness-construction/02-02-SUMMARY.md
@.planning/phases/03-formatter-refactor-validation/03-RESEARCH.md
@.planning/phases/03-formatter-refactor-validation/03-VALIDATION.md
@documents/canonical_naming_rules.md
@templates/js/scripts.js
@scripts/tests/formatter_vectors.json
@scripts/references/test_formatter.js
@scripts/tests/test_formatter.py
@scripts/tests/test_formatter_parity.py

<interfaces>
From templates/js/scripts.js:

```javascript
class InfiniteNumberFormatter {
    getTier1Name(index) {}
    getMilliPrefix(t2Index) {}
    getFullNameFromExponent(exponent) {}
    normalizeDisplayValue(mantissa, exponent) {}
    getLuaStyleAbbreviationSuffix(exponent) {}
    getAbbreviationFromExponent(exponent) {}
    buildReadableText(mantissa, exponent) {}
    buildAbbreviationText(mantissa, exponent) {}
    parseNotationInput(rawInput) {}
    convertInput(rawInput) {}
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract one shared exponent-to-group pipeline inside the formatter</name>
  <files>templates/js/scripts.js</files>
  <behavior>
    - Test 1: The canonical fixture cases `e3`, `e33`, `e3003`, `e3006`, and `e6003` keep the exact documented full names.
    - Test 2: Those same cases keep the exact documented abbreviations and formatted text outputs.
    - Test 3: The refactor does not change the public method names or the global `formatter` bootstrap used by the reader and game pages.
  </behavior>
  <action>Refactor `templates/js/scripts.js` so naming and abbreviation generation use a shared internal exponent-to-group pipeline instead of duplicating suffix-index derivation in multiple methods. Introduce bounded helper methods for deriving `suffixIndex`, splitting it into `t1Index` and `t2Index`, and normalizing the shared group math used by both `getFullNameFromExponent` and `getLuaStyleAbbreviationSuffix`. Keep the canonical formulas from `documents/canonical_naming_rules.md` unchanged, keep the existing public method names intact, and do not alter DOM wiring or the global `formatter` instance.</action>
  <verify>
    <automated>node scripts/references/test_formatter.js --json && python scripts/tests/test_formatter.py && python scripts/tests/test_formatter_parity.py</automated>
  </verify>
  <done>The formatter core computes canonical full names and abbreviations through one shared helper path while preserving all existing fixture outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route text builders through the refactored helpers and preserve boundary behavior</name>
  <files>templates/js/scripts.js</files>
  <behavior>
    - Test 1: Boundary windows `2994..3015`, `5994..6015`, `8994..9015`, and `29994..30015` remain duplicate-free for both readable names and abbreviations.
    - Test 2: Sparse sample-range parity remains green after the internal refactor.
    - Test 3: `buildReadableText` and `buildAbbreviationText` still normalize fixture cases like `mantissa: 1, exponent: 3005` to `100 Millillion` and `100 Mi`.
  </behavior>
  <action>Update `buildReadableText`, `buildAbbreviationText`, and any helper methods they rely on so they delegate cleanly to the new shared formatter pipeline. Preserve the existing normalization behavior and boundary outputs while removing internal drift risks such as separate suffix-index math or divergent group splitting logic. Keep the reader/game-facing output format stable and avoid introducing new public APIs.</action>
  <verify>
    <automated>node scripts/references/test_formatter.js --json && python scripts/tests/test_formatter_parity.py</automated>
  </verify>
  <done>The readable-text and abbreviation-text builders use the refactored helpers and all Phase 2 boundary and parity checks remain green.</done>
</task>

</tasks>

<verification>
The core refactor is complete when `templates/js/scripts.js` is internally cleaner but externally identical for every canonical fixture, boundary window, and parity sample already established in Phase 2.
</verification>

<success_criteria>
Phase 3 has a stable formatter core that still matches the canonical naming and abbreviation rules exactly and is safe to extend with bound enforcement in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-formatter-refactor-validation/03-01-SUMMARY.md`
</output>
