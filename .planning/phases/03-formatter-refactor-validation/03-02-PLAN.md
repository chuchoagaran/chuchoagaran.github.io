---
phase: 03-formatter-refactor-validation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["scripts/tests/formatter_vectors.json", "scripts/references/test_formatter.js", "templates/js/scripts.js"]
autonomous: true
requirements: ["FMT-03"]

must_haves:
  truths:
    - "Users can still convert the documented maximum supported input `1e2999999`."
    - "Users receive deterministic errors for out-of-range or unsafe integer exponents instead of undefined formatter output."
    - "Regression commands fail automatically if supported-ceiling or safe-integer validation drifts."
  artifacts:
    - path: "scripts/tests/formatter_vectors.json"
      provides: "Regression cases for supported-ceiling and invalid-input behavior."
    - path: "scripts/references/test_formatter.js"
      provides: "Automated parser and bounds verification in the JS harness."
    - path: "templates/js/scripts.js"
      provides: "Runtime supported-ceiling and safe-integer enforcement."
  key_links:
    - from: "scripts/tests/formatter_vectors.json"
      to: "scripts/references/test_formatter.js"
      via: "fixture-driven input and bounds assertions"
      pattern: "inputCases|parseCases|convertInput"
    - from: "scripts/references/test_formatter.js"
      to: "templates/js/scripts.js"
      via: "convertInput and parseNotationInput validation"
      pattern: "convertInput|parseNotationInput"
---

<objective>
Enforce the documented supported ceiling and safe-integer rules in the runtime formatter, and lock those behaviors into the existing regression harness.

Purpose: Phase 2 proved canonical outputs for valid cases, but the runtime still needs explicit guardrails so unsupported exponents cannot slip through as unreliable Number-based values.
Output: Bound-aware fixture coverage, JS harness validation for parser behavior, and runtime enforcement in `templates/js/scripts.js`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-test-harness-construction/02-01-SUMMARY.md
@.planning/phases/02-test-harness-construction/02-02-SUMMARY.md
@.planning/phases/03-formatter-refactor-validation/03-RESEARCH.md
@.planning/phases/03-formatter-refactor-validation/03-VALIDATION.md
@documents/canonical_naming_rules.md
@README.md
@templates/js/scripts.js
@scripts/tests/formatter_vectors.json
@scripts/references/test_formatter.js
@scripts/tests/test_formatter_parity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the shared fixture contract and JS harness for bounds validation</name>
  <files>scripts/tests/formatter_vectors.json, scripts/references/test_formatter.js</files>
  <action>Add a new fixture section for parser and bounds behavior, using explicit cases that cover: valid scientific input at `1e2999999`, out-of-range scientific input at `1e3000000`, unsafe scientific exponents beyond `Number.isSafeInteger`, malformed scientific notation, and suffix-derived exponents that exceed the supported ceiling. Update `scripts/references/test_formatter.js` so `--json` mode includes machine-readable results for these input cases and exits non-zero when parser outcomes drift. Reuse the existing fixture-first pattern instead of embedding new hard-coded validation arrays directly in the harness.</action>
  <verify>
    <automated>node scripts/references/test_formatter.js --json</automated>
  </verify>
  <done>The shared fixture contract and JS harness both encode supported-ceiling and invalid-input expectations for the formatter runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce supported-ceiling and safe-integer guards in runtime parsing</name>
  <files>templates/js/scripts.js</files>
  <behavior>
    - Test 1: `convertInput("1e2999999")` succeeds and still returns canonical readable and abbreviation text.
    - Test 2: `convertInput("1e3000000")` fails with a clear supported-range error.
    - Test 3: Scientific exponents that are not safe integers fail before naming or abbreviation logic runs.
    - Test 4: Suffix inputs whose derived exponent exceeds the supported ceiling also fail with the same bound enforcement.
    - Test 5: All existing Phase 2 fixture and parity commands remain green after the validation changes.
  </behavior>
  <action>Update `parseNotationInput` and any formatter guard helpers in `templates/js/scripts.js` so the runtime enforces the documented maximum supported exponent `2999999` and rejects unsafe integer exponents deterministically. Introduce explicit constants/helpers for the supported ceiling and safe-integer validation, apply them to both scientific-notation and suffix-based parsing flows, and keep error handling in the existing `{ ok: false, error }` shape so the current reader UI continues to work. Do not relax the documented bound and do not add any new dependencies.</action>
  <verify>
    <automated>node scripts/references/test_formatter.js --json && python scripts/tests/test_formatter.py && python scripts/tests/test_formatter_parity.py</automated>
  </verify>
  <done>The runtime rejects unsupported or unsafe inputs consistently while still passing every canonical fixture and parity command for supported values.</done>
</task>

</tasks>

<verification>
Phase 3 bounds enforcement is complete when supported valid inputs still format correctly, unsupported inputs fail deterministically, and the JS/Python regression commands stay green.
</verification>

<success_criteria>
The formatter runtime now matches both the canonical naming rules and the documented supported ceiling, closing the remaining safe-integer and unsupported-input gaps before milestone completion.
</success_criteria>

<output>
After completion, create `.planning/phases/03-formatter-refactor-validation/03-02-SUMMARY.md`
</output>